import setLang from '../lang'

const lang = {
  b: {
    locale: 'en',
    table: {
      confirmFilter: 'Confirm',
      resetFilter: 'Reset',
      clearFilter: 'All'
    }
  }
}

setLang(lang)

export default lang
