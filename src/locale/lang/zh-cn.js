import setLang from '../lang'

const lang = {
  b: {
    locale: 'zh-cn',
    table: {
      confirmFilter: '筛选',
      resetFilter: '重置',
      clearFilter: '全部'
    }
  }
}

setLang(lang)

export default lang
